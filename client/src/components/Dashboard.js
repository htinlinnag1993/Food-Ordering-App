import React, { Component } from 'react';
import { Container } from 'react-materialize';

class Dashboard extends Component {
    render() {
        return (
            <Container>
                <h1 className="black-text">Dashboard</h1>
            </Container>
        );
    }
}

export default Dashboard;