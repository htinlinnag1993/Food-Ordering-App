import React, { Component } from 'react';
import { Container } from 'react-materialize';

class Profile extends Component {
    render() {
        return (
            <Container>
                <h1 className="black-text">Profile</h1>
            </Container>
        );
    }
}

export default Profile;